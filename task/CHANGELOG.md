# Changelog

> Append-only — chỉ thêm dòng mới, không sửa/xóa dòng cũ. Ghi khi có **quyết định quan trọng, đổi hướng, hoặc sự cố đáng nhớ** — không ghi việc nhỏ nhặt hàng ngày. File này không nằm trong đường đọc mặc định của agent; chỉ mở khi cần tra lại lịch sử.

| Ngày | Thay đổi |
| --- | --- |
| _(điền ngày)_ | Tách `TASKS.md` (1 file, phình to dần) thành 3 file: `TASKS.md` (index nhẹ, ổn định), `CURRENT-TASK.md` (checklist bước đang làm, bị ghi đè khi chuyển bước), `CHANGELOG.md` (nhật ký append-only, không đọc mặc định) — lý do: tránh tốn ngữ cảnh khi agent/người đọc lại file theo thời gian dự án kéo dài |
| 2026-09-07 | Hoàn thành Bước 6 — API Contract cho Vertical Slice 4 endpoint (`GET /me`, `POST /health-profile`, `POST /locations`, `GET /aqi/current`) → `docs/09-API_CONTRACT.md`: quy ước chung (versioning `/api/v1`, UUID string, ISO 8601 UTC, format success/lỗi), bảng mã lỗi nghiệp vụ đối chiếu `BUSINESS-RULES.md`, DTO + JSON mẫu từng endpoint, chuỗi file tương tác theo tầng handler→service→repository, đối chiếu đủ nhánh rẽ 7 flow trong `USER-FLOWS.md` |
| 2026-09-09 | Hoàn thành Mục 1 (Bước 7) — backend `GET /api/v1/me`: implement `internal/auth` (jwks_client + token_verifier bằng `lestrrat-go/jwx/v2`, claims), `auth_middleware`, `user_repo` (FindByKeycloakID/Create), bản đọc `health_profile_repo`/`alert_threshold_repo`/`location_repo`, `user_sync_service` (FindOrCreate upsert + GetUserState), `user_handler.GetMe`, route đăng ký trong `cmd/api/main.go`, thêm `KEYCLOAK_ISSUER` vào `.env`. DoD đã test thật và pass 4/4 (401 khi thiếu token; JWT hợp lệ → tự tạo user + trả đúng shape mục 4.1 với profile/threshold null, locations []; gọi lại cùng JWT → cùng id, không nhân đôi user; field-set khớp 100%) — test trên scratch PostgreSQL 16 + OIDC JWKS/token harness tạm vì Keycloak Realm/Client chưa xác nhận (xem Rủi ro trong `TASKS.md`). Thêm dòng `package <tên>` cho 23 file scaffold Go đang rỗng để module compile được |
| 2026-09-10 | Quyết định: thêm `sensitivity_level` vào Onboarding UI (thay vì default ngầm) — cập nhật `WIREFRAMES.md` #2 (thêm segmented control "Bình thường"/"Nhạy cảm hơn"), `USER-FLOWS.md` flow 2, và cần tự cập nhật `API-CONTRACT.md` mục 4.2: đổi ghi chú field từ "MVP chưa thu thập trên UI" thành "Thu thập trực tiếp tại Onboarding bước 1; vẫn giữ optional + default `normal` ở tầng API để phòng gọi trực tiếp" |
| 2026-09-10 | Hoàn thành Mục 2 (Bước 7) — backend `POST /api/v1/health-profile`: ma trận ngưỡng 18 tổ hợp lưu dạng lookup table trong `health_profile_service.go` (không tính lại bằng if-else), custom `custom_threshold_aqi` ghi đè ma trận + `is_custom=true`; validate `[0,500]` ngoài cấu trúc → `400 VALIDATION_ERROR`, `[0,49]` → `422 THRESHOLD_TOO_LOW` (không clamp); upsert `health_profiles` + `alert_thresholds` trong **cùng 1 transaction** (update-in-place theo `BUSINESS-RULES.md` mục 0); thêm `UpsertByUserID` cho 2 repo, `health_profile_handler.Upsert`, route `POST /api/v1/health-profile`. DoD pass đủ 5/5 (unit test 18/18 tổ hợp + 3 tổ hợp test thật qua HTTP; update-in-place giữ nguyên profile id, DB còn 1 row mỗi bảng; `custom=49`/`0` → 422; enum sai/thiếu field/`501`/`-1`/`80.5` → 400; response field-set khớp 100% mục 4.2). Test trên scratch PostgreSQL 16 + OIDC harness tạm (Keycloak Realm/Client vẫn chưa xác nhận) |
